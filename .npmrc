# Evita fallos en Windows con el postinstall de @stellar/stellar-sdk (yarn setup || true).
# Si tienes Git para Windows, descomenta la línea siguiente para que los scripts usen bash:
# script-shell=C:\\Program Files\\Git\\bin\\bash.exe
